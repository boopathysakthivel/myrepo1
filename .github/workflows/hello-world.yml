name: hello world pipeline
on: push
jobs:
   my-job:
     runs-on: ubuntu-latest
     steps:
        - name: my-step
          run: echo "Hello World!"
        - name: my-world
          run: echo "Hello World 2"
